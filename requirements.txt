# Pinned to a verified-working set (Python 3.13, CPU). torchdata is no longer used.
# For a CUDA build of torch, install from https://download.pytorch.org/whl/cu124 instead.
torch==2.13.0
torchmetrics==1.9.0
torchinfo==1.8.0
numpy==2.5.1
pandas==3.0.3
tqdm>=4.66
